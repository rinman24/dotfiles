# ~/.config/tmux/tmux.conf — managed by chezmoi (rinman24/dotfiles)

# Terminal
set-option -g default-terminal 'tmux-256color'
set-option -g terminal-overrides ',xterm-256color:RGB'

# General options
set -g mouse on
set -g prefix ^A
set -g base-index 1           # start indexing windows at 1 instead of 0
set -g detach-on-destroy off  # don't exit from tmux when closing a session
set -g escape-time 0          # zero-out escape time delay
set -g history-limit 1000000  # increase history size (from 2,000)
set -g renumber-windows on    # renumber all windows when any window is closed
set -g set-clipboard on       # use system clipboard

# Key bindings
bind C-y display-popup -d "#{pane_current_path}" -E "bash"
bind C split-window -v -c "#{pane_current_path}" "claude --dangerously-skip-permissions"

# Plugins (TPM installs these under ~/.config/tmux/plugins/)
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'catppuccin/tmux#v2.3.0'
set -g @plugin 'tmux-plugins/tmux-cpu'
set -g @plugin 'tmux-plugins/tmux-battery'

# catppuccin config (set before TPM sources the plugin)
set -g @catppuccin_flavor 'mocha'
set -g @catppuccin_window_status_style 'rounded'

# Status line + catppuccin modules (deferred via #{E:...}, resolved at render)
set -g status-right-length 100
set -g status-left-length 100
set -g status-left ""
set -g status-right "#{E:@catppuccin_status_application}"
set -agF status-right "#{E:@catppuccin_status_cpu}"
set -agF status-right "#{E:@catppuccin_status_ram}"
set -ag  status-right "#{E:@catppuccin_status_session}"
set -ag  status-right "#{E:@catppuccin_status_uptime}"
set -agF status-right "#{E:@catppuccin_status_battery}"

# Initialize TPM — keep this line LAST
run '~/.config/tmux/plugins/tpm/tpm'
